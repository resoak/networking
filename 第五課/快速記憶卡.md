# 快速記憶卡

## 有線 LAN

- `Ethernet`：最常見有線區域網路。
- `100BaseT`：100 Mbps + Baseband + Twisted Pair。
- `T568A / T568B`：RJ-45 接腳標準。
- 電腦接交換器：通常直接線。

## 無線 LAN

- `802.11`：Wi‑Fi
- `Ad Hoc`：不用 AP，直接互連
- `Infrastructure`：透過 AP
- `SSID`：無線網路名稱
- `WPA2`：比 WEP 更常見也更安全
